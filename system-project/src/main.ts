// Ensure the correct import path and that '../system-project/system' exists and exports 'initializeSystem'
import { initializeSystem } from './system';

function main() {
    initializeSystem();
}

main();